Phase 1: The Foundation & The Vault
We actually just drafted the logic for this in our previous steps, but we need to officially lock in the Manifest to recognize our new lib folder.

manifest.json (Updated): We will rewrite this first to ensure all the new lib/ files load in the correct sequence.

lib/constants.js & lib/storage.js: (Already drafted) Our central configuration and secure storage wrappers.

options/options.html & options/options.js: (Already drafted) The settings page to securely save your API key.

Phase 2: The AI Intelligence Layer
Before we draw boxes on the screen, we need to ensure our extension knows how to talk to Gemini securely.
4. lib/gemini.js: A clean, isolated utility file that takes an image (Base64) and a prompt, formats the exact JSON payload Google requires, and executes the fetch() request.
5. background/service_worker.js: The "Brain." It will listen for you clicking the extension icon, tell YouTube to activate capture mode, and handle the secure routing between lib/gemini.js and your saved API key.

Phase 3: The Capture Mechanism (The Eyes)
Now we move to the front lines—injecting code into YouTube.
6. lib/dom.js: A utility file containing the complex math required to calculate mouse coordinates and the <canvas> logic to extract the video frame.
7. content/capture_mode.js: The script that throws the transparent "glass" overlay over YouTube, changes your cursor to a crosshair, and draws the visual bounding box as you drag your mouse.

Phase 4: The Interface & Orchestration (The Face)
Finally, we display the results to the user.
8. content/shadow_style.css: The custom CSS variables and basic structural styling for our popup, designed to live safely inside the Shadow DOM.
9. content/chat_ui.js: The script that builds the draggable chat window, displays the captured image, and handles the user typing follow-up questions.
10. content/injector.js: The "Traffic Cop." This script sits on the YouTube page, listens to the service_worker.js, and decides exactly when to trigger capture_mode.js and when to pop open chat_ui.js.